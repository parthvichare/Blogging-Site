<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blogs</title>
  </head>
  <body>
    <% blogs.forEach(blog => { %>
    <h1><%= blog.title %></h1>
    <a href="/blog/<%= blog._id %>">View</a>
    <% }) %>
<!-- 
    <script>
      // Function to get a specific cookie by name
      function getCookie(name) {
        const value = `; ${document.cookie}`;
        const parts = value.split(`; ${name}=`);
        if (parts.length === 2) return parts.pop().split(";").shift();
      }

      // Function to check if the user is authenticated (token stored in cookies)
      function checkAuth(blogId) {
        // Get the token from cookies (assuming your token is stored as 'token' in cookies)
        const token = getCookie("token");

        if (token) {
          // Token exists, allow navigation to the blog page
          window.location.href = `/blog/${blogId}`;
        } else {
          // No token, redirect to the sign-in page
          window.location.href = "/user/signin";
        }
      }
    </script> -->
  </body>
</html>
